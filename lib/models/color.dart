import 'package:flutter/material.dart';

class AppColors {
  Color backgroundColor = Colors.grey[900]!;
  Color mainTextColor = Colors.white;
  Color foregroundBlueColor = Colors.blue;
  Color mainRedColor = Colors.red;
  Color opacityTextColor = Colors.grey;
  Color dismissibleNoteBackgroundColor = Colors.grey[700]!.withOpacity(0.5);
  Color dismissibleNoteTextColor = Colors.white;
  Color appBarIconBackgroundColor = Colors.grey[700]!;
  Color appBarIconTextColor = Colors.white;
  Color appBarDisableIconTextColor = Colors.grey;
}
